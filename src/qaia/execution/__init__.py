"""FUTURE: agentic EXECUTION + self-healing module.

Intentionally empty in the MVP. The ``Executor`` seam lives in ``qaia.ports`` and
the sandbox whitelist in ``config/allowed_tools.yaml``. This package's emptiness
enforces that GENERATION has zero execution capability. See README.md.
"""
